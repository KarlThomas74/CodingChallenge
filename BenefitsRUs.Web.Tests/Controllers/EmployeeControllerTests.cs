namespace BenefitsRUs.Web.Tests.Controllers
{
    //If this were production code, Tests would go here to ensure that mappings were done
    //from viewmodels to domainmodels.

    //The controllers are testable with mock objects since we used IOC to create controllers 
    //with pluggable dependencies
    public class EmployeeControllerTests
    {
    }
}
